# Inquiry form delivery (api/inquiry.js)
# Create a key at https://resend.com/api-keys
RESEND_API_KEY=
# Where lead notifications land
INQUIRY_TO_EMAIL=
# Optional. Defaults to "G8 Flow <onboarding@resend.dev>", which needs no domain
# verification. Switch to an address on a verified domain when you have one.
INQUIRY_FROM_EMAIL=
